#include<stdio.h>
#include <stdlib.h>
int main()
{
int n,x;
int count=0;
scanf("%d",&n);
int num[n];
for(int i=0;i<n;i++)
{
    scanf("%d",&num[i]);
}
scanf("%d",&x);
for(int i=0;i<n;i++)
{
    if(num[i]==x)
    count++;
}
if(count==x)
printf("YES");
else
printf("NO");
}
