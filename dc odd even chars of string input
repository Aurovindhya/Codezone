#include<stdio.h>
#include <stdlib.h>

int main()
{
char str[101];
scanf("%s",str);
for(int i=0;i<strlen(str);i++)
{
    if(i%2==0)
    printf("%c",str[i]);
}printf("\n");
for(int i=0;i<strlen(str);i++)
{
    if(i%2!=0)
    printf("%c",str[i]);
}

}
