#include<stdio.h>
#include<stdlib.h>
#include<math.h>
int main()
{
unsigned long long int n,i;
int count=0;
scanf("%lld",&n);
if(n==2)
printf("yes");
for(i=2;i*i<=n;i++)
{
    if(n%i==0)
    {
    printf("no");
    return 0;
    }
    else
    printf("yes");
    return 0;
}
}
