#include<stdio.h>
#include <stdlib.h>
int bin(int x)
{
    if(x>0)
    {
        bin(x/2);
       printf("%d",(x%2));
    }
}
int main()
{
int n;
scanf("%d",&n);
for(int i=0;i<=n;i++)
{
    bin(i*i);
    printf(" ");
}

}
