#include<stdio.h>
#include <stdlib.h>
int main()
{
int n,i,temp;
scanf("%d",&n);
int count0=0,count1=0;
while(scanf("%d",&temp)==1)
{
    if(temp==0)
    count0++;
    else
    count1++;
}
for(i=0;i<count0;i++)
printf("0 ");
for(i=0;i<count1;i++)
printf("1 ");
}
